namespace ImGui
{
    // Per column data for Columns()
    internal struct ImGuiColumnData
    {
        public float OffsetNorm;     // Column start offset, normalized 0.0 (far left) -> 1.0 (far right)
                                     //float     IndentX;
    }
}
